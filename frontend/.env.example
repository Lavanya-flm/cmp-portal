# Use relative path to go through Vite proxy (avoids CORS in dev)
VITE_API_BASE_URL=/api/v1

# Or use direct URL if running without the Vite dev server
# VITE_API_BASE_URL=http://localhost:3000/api/v1
